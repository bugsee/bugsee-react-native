<?xml version="1.0" encoding="utf-8"?>
<!-- No permissions, no components: the SDK's own manifest supplies those and
     is merged in through the `api` dependency. This exists because AGP needs
     a manifest for the library's namespace to resolve. -->
<manifest />
